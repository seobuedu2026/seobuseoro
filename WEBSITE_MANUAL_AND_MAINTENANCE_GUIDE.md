# 📘 서부서로 수업성장 캘린더 — 종합 기능 명세 및 운영·유지관리 가이드

본 문서는 **2026학년도 2학기 서부서로 수업성장 캘린더([seobuseoro.web.app](https://seobuseoro.web.app))**의 전체 기능 명세, Firebase 무료(Spark) 요금제 기반의 트래픽/사용자 수용 한도 분석, 그리고 향후 지속 가능한 유지관리 방안을 정리한 기술 가이드입니다.

---

## 1. 🌟 사이트 전체 개발 및 활성화 기능 명세

### 1-1. 🗓️ [캘린더 탭] — 수업성장 캘린더
* **월별 단일 뷰 & N개월 모아보기 (포스터 뷰) 전환**:
  * 상단 칩을 통해 특정 월(예: 9월, 10월, 11월) 단일 집중 뷰와 포스터 형태의 모아보기 화면을 1클릭으로 전환.
  * 모아보기 선택 시 관리자가 지정한 월들이 화면에 균형 있게 그리드로 배치.
* **관리자 전용 캘린더 제어 시스템**:
  * **[달력 추가/관리]**: 기준 연도(2025~2030학년도 자동 지원), 상단 탭에 노출할 월(1~12월), 모아보기에 포함할 월을 관리자가 직접 체크하여 실시간 설정.
  * **[월별 소제목 및 강조 안내 문구 직접 수정]**: 월별 테마(예: "수다박스의 달", "수업나눔 주간" 등)와 강조 주간 배너 문구를 모달에서 즉시 편집.
  * **[날짜 빈칸 퀵 등록 (+)]**: 관리자 로그인 상태에서 달력의 빈 날짜 셀에 마우스를 올리면 `+` 버튼이 활성화되어 즉시 새 행사를 등록.
  * **[행사 드래그 앤 드롭 (Drag & Drop)]**: 등록된 행사 칩을 마우스로 끌어서 다른 날짜로 이동하면 일정이 즉시 변경 및 저장.
  * **[엑셀(.xlsx) 파일 일괄 등록]**: 교육청 행사 엑셀 파일을 업로드하면 자동으로 날짜, 행사명, 장소, 시간, 신청링크 등을 파싱하여 일괄 등록.
  * **[프로그램 유형(카테고리/색상) 관리]**: 연수·워크숍, 특강, 멘토링 등 기존 카테고리 외에 새로운 유형 추가, 색상 변경, 명칭 편집 가능.
* **행사 상세 정보 모달 (팝업)**:
  * 행사 클릭 시 날짜, 시간, 장소, 대상, 신청 방법, 신청 링크 바로가기, D-Day 카운트다운 배너 확인 가능.
* **하단 인쇄물 공식 색상 범례 & 안내 문구**:
  * 인쇄물(포스터)과 동일한 공식 카테고리 배지가 표시되며, 바로 아래에 `※ 세부 일정 및 장소는 학교 공문 및 신청 링크를 통해 확인하시기 바랍니다.` 안내 문구 배치.

---

### 1-2. 📋 [프로그램 탭] — 한눈에 보기
* **월별 & 유형별 다중 필터링**:
  * 월 필터(전체, 9월, 10월, 11월 등)와 카테고리 필터(전체, 연수·워크숍, 특강 등)를 교차 선택하여 원하는 연수만 빠르게 탐색.
* **반응형 프로그램 카드 그리드**:
  * 행사명, 일시, 장소, 대상, 신청 링크 버튼이 깔끔한 카드 형태로 렌더링.
* **관리자 빠른 추가**:
  * 상단에 `[새 프로그램 추가]` 및 `[유형 추가·관리]` 버튼이 구비되어 즉각적인 데이터 관리 지원.

---

### 1-3. 💬 [참여후기 탭] — 실시간 소통 및 후기 게시판
* **참여 후기 등록**:
  * 교원이 참여한 행사를 드롭다운에서 선택하고 소감 및 수업 적용 나눔을 등록.
  * 비로그인 교원도 `작성자 성함`과 `비밀번호`만 입력하면 간편 등록 가능.
* **개인정보 보호 (성함 마스킹 처리)**:
  * 일반 교원 화면에서는 성함의 첫 글자만 노출되고 나머지는 마스킹 처리 (`김세찬` ➔ `김**`).
  * 관리자 로그인 시에는 전체 성함을 확인 가능.
* **관리자 사전 승인제 시스템 (Pending Review)**:
  * 후기 등록 시 기본 `pending(승인대기)` 상태로 저장되며, 관리자가 승인하기 전까지는 일반 화면에 노출되지 않음.
  * 작성자 본인에게는 "작성하신 후기는 관리자의 승인을 기다리고 있습니다" 안내 배너 표시.
  * 관리자는 1클릭으로 `[승인]` / `[승인취소]` 처리 가능.
* **작성자 전용 후기 수정 및 삭제**:
  * 등록 폼 하단 **[✏️ 후기 수정하기]** 버튼 클릭 ➔ 성함과 비밀번호 입력 후 본인 후기 조회 ➔ 줄바꿈(엔터)이 자유로운 텍스트에어리어에서 내용 수정 또는 삭제.
  * 작성자가 내용을 수정하면 **자동으로 다시 `pending(관리자 재승인 대기)` 상태로 전환**되어 무단 변조 방지.
* **페이지네이션 & 공감 기능**:
  * 후기를 **한 페이지에 10개씩** 깔끔하게 나누어 보여주는 페이지 번호 네비게이션 제공.
  * 마음에 드는 후기에 `❤️ 공감` 버튼 클릭 시 실시간 카운트 반영.

---

### 1-4. 📚 [수업나눔방 탭] — 패들렛(Padlet) 및 자료실
* **학교급·영역별 패들렛 바로가기 허브**:
  * 초등, 중등, 특수, 영역별 수업나눔 및 자료 공유 패들렛 링크를 카드 형태로 제공하여 새 창으로 안전하게 연결.

---

### 1-5. 🔐 [글로벌 인증 및 푸터 시스템]
* **Firebase & Google OAuth 2.0 관리자 인증**:
  * 우측 상단 및 푸터의 `[관리자 로그인]`을 통해 지정된 관리자 계정으로 로그인 시 모든 관리 기능(수정/삭제/추가/승인) 활성화.
* **스마트 캐시 무효화 (Cache Busting)**:
  * 배포 시 브라우저 캐시로 인한 구버전 노출 문제를 방지하기 위해 버전 파라미터(`?v=...`) 자동 관리.

---

## 2. 💰 Firebase Spark(무료) 요금제 수용 용량 및 트래픽 분석

본 홈페이지는 구글의 **Firebase Spark(평생 무료) 플랜**을 사용하고 있습니다.

| Firebase 서비스 | Spark 플랜 무료 제공량 | 실제 월간 수용 가능 인원 및 트래픽 |
| :--- | :--- | :--- |
| **1. Firebase Hosting**<br>(웹사이트 정적 호스팅) | • 스토리지: **10 GB**<br>• 네트워크 전송량: **월 10 GB** (일 360MB) | • **월 약 50,000 ~ 100,000회 페이지 뷰(PV)**<br>• 월간 순방문자(MAU) 기준 **약 10,000 ~ 30,000명** 수용 |
| **2. Cloud Firestore**<br>(후기 및 실시간 데이터베이스) | • 스토리지: **1 GB**<br>• 읽기(Read): **50,000회 / 일**<br>• 쓰기(Write): **20,000회 / 일**<br>• 삭제(Delete): **20,000회 / 일** | • **하루 1,000 ~ 3,000명**이 동시에 후기를 조회하고 작성해도 무료 한도 내에서 여유롭게 작동<br>• 후기 텍스트 수만 건 이상 영구 보관 가능 |
| **3. Firebase Authentication**<br>(구글 로그인 인증) | • 월간 활성 사용자(MAU): **50,000명 / 월** | • 관리자 및 교원 로그인 **월 5만 명까지 완전 무료** |

### 📊 결론: 교육청/지원청 단위 운영에 100% 충분한 용량
* 서울 서부 관내 유·초·중·고 전체 교원 수(약 6,000 ~ 8,000명)가 매일 동시에 접속하여 후기를 열람하고 캘린더를 확인하더라도 **무료(Spark) 한도의 10~20% 내외만 소모**됩니다.
* 추가 비용 발생 우려 없이 **0원으로 안정적이고 쾌적하게 영구 운영**이 가능합니다.

---

## 3. 🛠️ 지속 가능한 운영 및 유지관리(Maintenance) 가이드

### 3-1. 일상 운영 (웹 화면에서 100% 처리 가능한 작업)
개발 도구나 코딩 없이, 웹 브라우저에서 **관리자 로그인** 후 모든 일상 관리가 가능합니다.

1. **신규 행사 등록 및 수정**:
   * 캘린더에서 해당 날짜를 누르거나 프로그램 탭의 `[새 프로그램 추가]` 클릭.
   * 엑셀 파일이 있을 경우 캘린더 우측 상단 `[엑셀 파일 등록]`을 통해 몇 초 만에 수십 개 행사 일괄 업로드.
2. **참여 후기 관리**:
   * 참여후기 탭에서 교원들이 등록한 후기 중 적합한 글에 `[승인]` 버튼을 눌러 전체 공개 처리.
   * 부적절한 후기는 `[승인취소]` 또는 수정/삭제 모달에서 삭제.
3. **새 학년도 / 새 학기 갱신**:
   * `[달력 추가]` 버튼 ➔ `기준 연도 선택`에서 연도만 변경하면(예: 2027년) **법정 공휴일(추석, 설날, 개천절 등)과 요일이 자동으로 100% 매핑**됩니다.
   * 상단에 표시할 월과 모아보기에 포함할 월을 체크박스로 선택 후 적용.

---

### 3-2. 소스코드 수정 및 배포 절차 (개발자/관리자용)

새로운 기능 추가나 디자인 수정 시 아래 표준 절차를 따릅니다.

```bash
# 1. 프로젝트 폴더로 이동 (VS Code 또는 터미널)
cd c:\Users\gogh9\Documents\Antigravity_Project\SB

# 2. 코드 수정 후 Git 커밋 & 푸시
git add .
git commit -m "feat: 새로운 기능 추가 또는 UI 수정"
git push origin main

# 3. Firebase Hosting 즉시 배포 (10초 소요)
firebase deploy --only hosting
```

---

### 3-3. 데이터 백업 및 보안 관리 수칙

1. **관리자 계정 관리**:
   * `js/auth/googleAuth.js` 내에 허용된 관리자 이메일 목록(`ADMIN_EMAILS`)이 정의되어 있습니다. 담당자가 변경될 경우 해당 이메일 주소만 업데이트하면 됩니다.
2. **데이터 백업**:
   * 필요 시 엑셀 파일 형태로 행사 일정을 보관하거나, Firebase 콘솔(`console.firebase.google.com`)의 Firestore 탭에서 데이터를 백업할 수 있습니다.
